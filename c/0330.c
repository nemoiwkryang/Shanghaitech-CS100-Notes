/*
pointer arithmetic- p+i points to the address i*sizeof(Type) after p, a[i] if array
same, i+/-p, p+/-=, ++/--p, p++/--
a=&a[0] if a Type[N]
*/

#include<stdio.h>
#include<stdlib.h>
#include<stdbool.h>

int main(void){
    return 0;
}